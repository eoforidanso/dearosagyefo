[build]
  command = "npm run build"
  publish = "."

# Redirect API calls to your Railway backend
[[redirects]]
  from = "/api/*"
  to = "https://your-app-name.up.railway.app/api/:splat"
  status = 200
  force = true

# Handle client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
